# Умное хранилище сканов

Полнофункциональное веб-приложение для управления сканами документов с автоматическим OCR-распознаванием, поиском по содержимому и интеллектуальным извлечением метаданных (даты, суммы, имена, контакты).

## Функциональность

- **📤 Загрузка документов**: Drag-and-drop загрузка сканов в форматах JPEG, PNG, TIFF, BMP, WebP
- **🔍 OCR-распознавание**: Автоматическое извлечение текста из изображений с использованием Tesseract.js
- **💰 Поиск по суммам**: Интеллектуальный поиск документов по денежным суммам
- **📅 Поиск по датам**: Поиск по датам в различных форматах
- **👤 Поиск по именам**: Извлечение и поиск персональных данных
- **📧 Извлечение контактов**: Автоматический поиск email и телефонов
- **🔗 Авто-склейка страниц**: Объединение нескольких страниц в один документ
- **🔐 JWT-авторизация**: Безопасная аутентификация пользователей
- **📱 Адаптивный интерфейс**: Современный UI с Tailwind CSS

## Технологический стек

### Backend
- **Node.js** + **Express.js**
- **SQLite** (better-sqlite3) — локальная база данных
- **JWT** — аутентификация
- **Tesseract.js** — OCR-распознавание
- **Multer** — загрузка файлов
- **bcryptjs** — хеширование паролей

### Frontend
- **React 18** + **Vite**
- **React Router v6**
- **Tailwind CSS**
- **Axios**

## Установка и запуск

### Требования
- Node.js версии 18 или выше
- npm или yarn

### Быстрый старт

1. Клонируйте репозиторий и перейдите в директорию проекта:
```bash
cd smart-scan-storage
```

2. Установите зависимости для всего проекта:
```bash
npm run install:all
```

3. Создайте файл `.env` в директории `server/`:
```bash
cp server/.env.example server/.env
```

4. Запустите сервер разработки:
```bash
npm run dev
```

Приложение будет доступно по адресам:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001

### Раздельный запуск

Запуск только сервера:
```bash
cd server && npm run dev
```

Запуск только клиента:
```bash
cd client && npm run dev
```

## Структура проекта

```
smart-scan-storage/
├── client/                 # React фронтенд
│   ├── src/
│   │   ├── components/     # Переиспользуемые компоненты
│   │   ├── context/        # React контексты (Auth, Toast)
│   │   ├── pages/          # Страницы приложения
│   │   ├── services/       # API сервисы
│   │   ├── App.jsx         # Главный компонент
│   │   └── main.jsx        # Точка входа
│   ├── package.json
│   └── vite.config.js
├── server/                 # Node.js бэкенд
│   ├── src/
│   │   ├── config/         # Конфигурация БД
│   │   ├── middleware/     # Middleware (аутентификация)
│   │   ├── routes/         # API маршруты
│   │   ├── services/       # Бизнес-логика (OCR)
│   │   └── index.js        # Главный файл сервера
│   ├── uploads/            # Загруженные файлы
│   ├── data/               # База данных SQLite
│   ├── package.json
│   └── .env
└── package.json            # Корневой package.json
```

## API Endpoints

### Аутентификация
| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/auth/register` | Регистрация пользователя |
| POST | `/api/auth/login` | Вход в систему |
| GET | `/api/auth/me` | Информация о текущем пользователе |
| PUT | `/api/auth/profile` | Обновление профиля |
| POST | `/api/auth/logout` | Выход |

### Документы
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/documents` | Список документов |
| GET | `/api/documents/:id` | Детали документа |
| POST | `/api/documents` | Загрузка документов |
| PUT | `/api/documents/:id` | Обновление документа |
| DELETE | `/api/documents/:id` | Удаление документа |
| PUT | `/api/documents/:id/pages/:pageId` | Обновление OCR текста страницы |

### Поиск
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/search` | Расширенный поиск |
| GET | `/api/search/suggestions` | Подсказки для автодополнения |
| GET | `/api/search/advanced` | Поиск по конкретным полям |

## База данных SQLite

База данных автоматически создаётся при первом запуске сервера в директории `server/data/smart-scan.db`.

### Таблицы

**users** — пользователи
- id, email, password_hash, name, created_at, updated_at

**documents** — документы
- id, user_id, title, description, category, tags, status, created_at, updated_at

**document_pages** — страницы документов
- id, document_id, file_name, original_name, file_path, file_size, mime_type, page_order, ocr_text, extracted_data, created_at

## Демонстрационные данные

После запуска вы можете:
1. Зарегистрировать нового пользователя
2. Войти в систему
3. Загрузить изображения документов
4. Дождаться OCR-обработки
5. Искать по тексту, датам, суммам

## Разработка

### Кодстайл
- JavaScript с ES6+ синтаксисом
- Форматирование Prettier (опционально)
- Комментарии на русском языке для пользовательских сообщений

### Тестирование
```bash
# Запуск сервера в режиме разработки
npm run dev:server

# Запуск клиента в режиме разработки
npm run dev:client

# Одновременный запуск
npm run dev
```

## Лицензия

MIT License

## Автор

MiniMax Agent
